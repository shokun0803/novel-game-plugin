# リリースプロセスドキュメント

**Novel Game Plugin リリース手順書**

## 目次

- [目的](#目的)
- [前提条件](#前提条件)
- [バージョン管理ルール](#バージョン管理ルール)
- [リリース手順](#リリース手順)
  - [1. 翻訳ファイルの更新](#1-翻訳ファイルの更新)
  - [2. バージョン情報の更新](#2-バージョン情報の更新)
  - [3. CHANGELOGの更新](#3-changelogの更新)
  - [4. ローカル検証](#4-ローカル検証)
  - [5. dev → master マージ](#5-dev--master-マージ)
  - [6. タグ付け](#6-タグ付け)
  - [7. 配布用ZIPの作成](#7-配布用zipの作成)
  - [8. GitHub Releaseの作成](#8-github-releaseの作成)
- [チェックリスト](#チェックリスト)
- [テスト観点](#テスト観点)
- [トラブルシューティング](#トラブルシューティング)
- [自動化提案](#自動化提案)
- [参考リンク](#参考リンク)

---

## 目的

このドキュメントは、Novel Game Plugin のリリース工程を標準化し、以下を実現します：

- **再現可能性**: 誰が見ても同じ手順でリリースできる
- **品質保証**: 翻訳漏れ・バージョン不整合を防止
- **履歴の整合性**: dev → master fast-forward 運用で履歴を書き換えない
- **安全性**: force-push / rebase などの危険な操作を排除

---

## 前提条件

### 必要な環境

- Git クライアント（2.0以上）
- WordPress テスト環境（4.7以上）
- PHP 7.0以上
- GNU gettext ツール（msgfmt コマンド）
- zip コマンド（または互換ツール）

### 権限要件

- リポジトリへの push 権限（master / dev ブランチ）
- GitHub Release 作成権限

### ブランチ運用ポリシーの確認

必ず `.github/copilot-instructions.md` の「ブランチ運用ポリシー」セクションを確認してください：

- `master`: リリース済み安定版（直接コミット禁止）
- `dev`: 統合開発ブランチ（リモートは master と同期状態を維持）
- リリース時は dev → master への fast-forward マージのみ

---

## バージョン管理ルール

本プラグインは **Semantic Versioning 2.0.0** に準拠します。

### バージョン番号の形式

```
MAJOR.MINOR.PATCH
```

#### MAJOR バージョン（互換性のない変更）

以下の場合に更新：

- データベーススキーマの非互換な変更
- 既存ゲームデータの自動移行が不可能な構造変更
- 削除された機能・API
- WordPress 最小要件の大幅な引き上げ

**例**: 投稿タイプ名変更、メタキー構造の変更

#### MINOR バージョン（機能追加）

以下の場合に更新：

- 新機能の追加（既存機能は維持）
- 既存データと互換性のある機能拡張
- 非推奨機能のマーク（削除はしない）

**例**: エクスポート/インポート機能追加、新しい管理画面の追加

#### PATCH バージョン（バグ修正）

以下の場合に更新：

- バグ修正のみ
- セキュリティ修正
- 翻訳の更新
- ドキュメントの修正

**例**: UI の文言修正、権限チェックの追加、翻訳ファイルの更新

### 判断基準

**迷った場合の原則:**

- ユーザーのアクションが必要か？ → MAJOR
- 新しいことができるか？ → MINOR  
- 動作が改善されるか？ → PATCH

---

## リリース手順

### 1. 翻訳ファイルの更新

#### 1.1 現在の翻訳ファイルをバックアップ

```bash
cd /path/to/novel-game-plugin
cp languages/novel-game-plugin.pot languages/novel-game-plugin.pot.bak
```

#### 1.2 .pot ファイルの再生成

WordPress の WP-CLI を使用する方法（推奨）:

```bash
# WP-CLI がインストールされている場合
wp i18n make-pot . languages/novel-game-plugin.pot \
  --domain=novel-game-plugin \
  --exclude=node_modules,vendor,.git,tests
```

**注意**: WP-CLI が利用できない場合は、以下の手動確認を実施：

1. すべての `__()`, `_e()`, `_x()` 関数の使用箇所を確認
2. 新規追加された文字列が `.pot` に含まれているか確認
3. 既存の `.pot` から新規文字列を手動で追加

#### 1.3 .pot ファイルのバージョン更新

`languages/novel-game-plugin.pot` を開き、以下を更新：

```po
"Project-Id-Version: Novel Game Plugin X.Y.Z\n"
"POT-Creation-Date: YYYY-MM-DD HH:MM+0000\n"
```

#### 1.4 複数参照の統合ルール

gettext は同じ msgid が複数ファイルで使用されている場合、参照行を統合します：

```po
#: admin/game-manager.php:23 admin/dashboard.php:22 admin/ad-management.php:40
msgid "You do not have permission to access this page."
msgstr ""
```

これは正常な動作であり、修正不要です。

#### 1.5 .po ファイルへの差分適用

```bash
# 日本語翻訳の更新
msgmerge --update languages/novel-game-plugin-ja.po languages/novel-game-plugin.pot
```

新規追加された msgid に対して翻訳を追加：

```po
#: admin/new-feature.php:10
msgid "New Feature"
msgstr "新機能"
```

#### 1.6 .mo ファイルの生成

```bash
# 日本語翻訳のコンパイル
msgfmt -o languages/novel-game-plugin-ja.mo languages/novel-game-plugin-ja.po
```

#### 1.7 翻訳の検証

```bash
# 未訳文字列の確認
msgattrib --untranslated languages/novel-game-plugin-ja.po

# fuzzy マーク（要確認）がある文字列の確認
msgattrib --only-fuzzy languages/novel-game-plugin-ja.po
```

**重要**: 未訳文字列や fuzzy マークがある場合は、翻訳を完了させてから次のステップへ進んでください。

---

### 2. バージョン情報の更新

以下の3箇所を **同じバージョン番号** に更新してください。

#### 2.1 novel-game-plugin.php のヘッダー

```php
/**
 * Plugin Name: Novel Game Plugin
 * Version: X.Y.Z
 */
```

**行番号**: 6行目付近

#### 2.2 novel-game-plugin.php の定数

```php
if ( ! defined( 'NOVEL_GAME_PLUGIN_VERSION' ) ) {
    define( 'NOVEL_GAME_PLUGIN_VERSION', 'X.Y.Z' );
}
```

**行番号**: 26行目付近

#### 2.3 バージョンの一致確認

```bash
# ヘッダーのバージョン
grep "^ \* Version:" novel-game-plugin.php

# 定数のバージョン
grep "define( 'NOVEL_GAME_PLUGIN_VERSION'" novel-game-plugin.php
```

両方が同じバージョン番号であることを確認してください。

---

### 3. CHANGELOGの更新

#### 3.1 Unreleased セクションを確認

`CHANGELOG.md` の `[Unreleased]` セクションに、今回のリリースに含める変更がすべて記載されているか確認します。

#### 3.2 新しいバージョンセクションを追加

```markdown
## [X.Y.Z] - YYYY-MM-DD

### Added (追加機能)
- 新機能の説明

### Changed (変更)
- 変更内容の説明

### Fixed (バグ修正)
- 修正内容の説明

### Removed (削除)
- 削除された機能の説明

## [Unreleased]
（次のリリース用の空セクション）
```

#### 3.3 移行ノートの追加（必要な場合）

MAJOR または MINOR バージョンの場合、移行時の注意事項を追加：

```markdown
### Migration Notes (移行時の注意事項)

このアップデートは後方互換性を維持していますが、以下の点に注意してください：

1. **新機能の利用方法**
   - 設定画面で○○を有効化してください

2. **データの互換性**
   - 既存のゲームデータは自動的に新形式に変換されます
```

---

### 4. ローカル検証

#### 4.1 WordPress テスト環境での動作確認

```bash
# テスト環境にプラグインをデプロイ
rsync -av --exclude='.git' . /path/to/test-wp/wp-content/plugins/novel-game-plugin/

# WordPress 管理画面で確認:
# - プラグイン情報画面でバージョンが正しいか
# - 翻訳が正しく読み込まれているか
# - 新機能が動作するか
# - 既存のゲームデータが正常に動作するか
```

#### 4.2 翻訳の動作確認

```php
// WordPress管理画面 → 設定 → 一般 → サイトの言語 を「日本語」に設定
// プラグインの管理画面で翻訳が適用されているか確認
```

#### 4.3 Git コミット

すべての変更を確認し、コミット：

```bash
git add languages/*.pot languages/*.po languages/*.mo
git add novel-game-plugin.php
git add CHANGELOG.md
git commit -m "Prepare release X.Y.Z

- Update version to X.Y.Z
- Regenerate translation files
- Update CHANGELOG"
```

---

### 5. dev → master マージ

#### 5.1 リモートの最新状態を取得

```bash
git fetch --prune origin
```

#### 5.2 master ブランチに切り替え

```bash
git checkout master
git pull origin master
```

#### 5.3 dev を master に fast-forward マージ

```bash
# fast-forward マージ（履歴の書き換えなし）
git merge --ff-only origin/dev
```

**注意**: `--ff-only` オプションにより、fast-forward できない場合はエラーになります。その場合は dev の履歴を確認し、リベースが必要かユーザーに確認してください。

#### 5.4 master をリモートに push

```bash
git push origin master
```

---

### 6. タグ付け

#### 6.1 アノテーテッドタグの作成

```bash
git tag -a vX.Y.Z -m "Release version X.Y.Z

主な変更点:
- 新機能の追加
- バグ修正
"
```

**重要**: タグ名には必ず `v` プレフィックスを付けてください（例: `v1.3.0`）。

#### 6.2 タグをリモートに push

```bash
git push origin vX.Y.Z
```

#### 6.3 タグの確認

```bash
# ローカルのタグ一覧
git tag -l

# リモートのタグ確認
git ls-remote --tags origin
```

---

### 7. 配布用ZIPの作成

#### 7.1 クリーンな作業ディレクトリの準備

```bash
# 一時ディレクトリを作成
mkdir -p /tmp/novel-game-plugin-release
cd /tmp/novel-game-plugin-release

# タグからクローン
git clone --depth 1 --branch vX.Y.Z https://github.com/shokun0803/novel-game-plugin.git
cd novel-game-plugin
```

#### 7.2 不要なファイルの削除

```bash
# Git 関連ファイル
rm -rf .git .gitignore .github

# 開発用ファイル
rm -rf tests scripts docs/*.md

# 一時ファイル
find . -name ".DS_Store" -delete
find . -name "*.bak" -delete
find . -name "*~" -delete
```

**保持するファイル**:

- すべての `.php` ファイル
- `languages/` ディレクトリ（.pot, .po, .mo）
- `css/`, `js/`, `assets/` ディレクトリ
- `templates/` ディレクトリ
- `README.md`, `CHANGELOG.md`
- `uninstall.php`

#### 7.3 ZIPファイルの作成

```bash
cd ..
zip -r novel-game-plugin-X.Y.Z.zip novel-game-plugin/ \
  -x "*.git*" "*/tests/*" "*/scripts/*" "*/docs/*"
```

#### 7.4 チェックサムの生成

```bash
# SHA256ハッシュの生成
sha256sum novel-game-plugin-X.Y.Z.zip > novel-game-plugin-X.Y.Z.zip.sha256

# 内容の確認
cat novel-game-plugin-X.Y.Z.zip.sha256
```

#### 7.5 ZIPファイルの検証

```bash
# ZIPの内容を確認
unzip -l novel-game-plugin-X.Y.Z.zip | head -20

# 不要なファイルが含まれていないか確認
unzip -l novel-game-plugin-X.Y.Z.zip | grep -E '\.git|tests/|scripts/'

# サイズの確認（目安: 500KB〜2MB程度）
ls -lh novel-game-plugin-X.Y.Z.zip
```

---

### 8. GitHub Releaseの作成

#### 8.1 GitHub Releases ページへアクセス

```
https://github.com/shokun0803/novel-game-plugin/releases/new
```

#### 8.2 Release 情報の入力

**Tag version**: `vX.Y.Z`（既存のタグを選択）

**Release title**: `Novel Game Plugin vX.Y.Z`

**Description**: 以下のテンプレートを使用

```markdown
## 📦 Novel Game Plugin vX.Y.Z

### 主な変更点

#### 追加機能
- 新機能の説明

#### 変更
- 変更内容の説明

#### バグ修正
- 修正内容の説明

### 移行時の注意事項

このアップデートは後方互換性を維持しています。

**アップグレード前の推奨事項:**
1. データベースのバックアップを取得
2. テスト環境で動作確認

### インストール方法

1. 下記の ZIP ファイルをダウンロード
2. WordPress 管理画面 → プラグイン → 新規追加 → プラグインのアップロード
3. ZIP ファイルを選択してインストール
4. プラグインを有効化

### ファイル情報

**ファイル名**: `novel-game-plugin-X.Y.Z.zip`  
**サイズ**: XXX KB  
**SHA256**: `<チェックサム値をここに貼り付け>`

### サポート

- [GitHub Issues](https://github.com/shokun0803/novel-game-plugin/issues)
- [ドキュメント](https://github.com/shokun0803/novel-game-plugin/blob/master/README.md)

---

**Full Changelog**: https://github.com/shokun0803/novel-game-plugin/blob/master/CHANGELOG.md
```

#### 8.3 ZIPファイルのアップロード

1. "Attach binaries" セクションに以下をドラッグ&ドロップ：
   - `novel-game-plugin-X.Y.Z.zip`
   - `novel-game-plugin-X.Y.Z.zip.sha256`

#### 8.4 リリースの公開

- **Set as the latest release** にチェックを入れる
- **Publish release** ボタンをクリック

---

## チェックリスト

リリース前に以下をすべて確認してください：

### 翻訳

- [ ] `.pot` ファイルを再生成した
- [ ] `.pot` のバージョン番号を更新した
- [ ] `.po` ファイルに差分を反映した
- [ ] 新規文字列の翻訳を追加した
- [ ] `.mo` ファイルを再生成した
- [ ] 未訳文字列がないことを確認した（`msgattrib --untranslated`）
- [ ] WordPress 管理画面で翻訳が正しく表示されることを確認した

### バージョン情報

- [ ] `novel-game-plugin.php` ヘッダーのバージョンを更新した
- [ ] `NOVEL_GAME_PLUGIN_VERSION` 定数を更新した
- [ ] 両方のバージョン番号が一致することを確認した

### CHANGELOG

- [ ] `CHANGELOG.md` に新しいバージョンセクションを追加した
- [ ] 変更内容を Added / Changed / Fixed / Removed に分類した
- [ ] 移行ノート（必要な場合）を追加した
- [ ] リリース日を記載した

### 動作確認

- [ ] WordPress テスト環境でプラグインが有効化できる
- [ ] 既存のゲームデータが正常に動作する
- [ ] 新機能が正しく動作する
- [ ] 翻訳が正しく表示される
- [ ] JavaScript エラーがない（ブラウザコンソール確認）
- [ ] PHP エラーがない（`WP_DEBUG` 有効で確認）

### Git 操作

- [ ] すべての変更をコミットした
- [ ] dev → master に fast-forward マージした
- [ ] master にタグを付けた（`vX.Y.Z` 形式）
- [ ] タグをリモートに push した

### 配布用ZIP

- [ ] クリーンな環境で ZIP を作成した
- [ ] 不要なファイル（.git, tests, scripts）を除外した
- [ ] ZIP の内容を確認した（`unzip -l`）
- [ ] SHA256 チェックサムを生成した
- [ ] ZIP ファイルサイズが妥当である（500KB〜2MB程度）

### GitHub Release

- [ ] Release ページで ZIP をアップロードした
- [ ] Release 説明文を記載した（テンプレート使用）
- [ ] SHA256 チェックサムを記載した
- [ ] "Set as the latest release" にチェックした
- [ ] Release を公開した

---

## テスト観点

### 基本動作テスト

#### プラグインのインストール

- [ ] ZIP ファイルからインストールできる
- [ ] プラグイン有効化が成功する
- [ ] プラグイン情報画面でバージョンが正しい

#### 翻訳の読み込み

- [ ] 日本語環境で管理画面の文言が日本語で表示される
- [ ] フロントエンドの文言が日本語で表示される
- [ ] 新規追加した文字列が翻訳されている

#### 既存データの互換性

- [ ] 既存のゲームが正常に表示される
- [ ] 既存のシーンが正常に編集できる
- [ ] 既存のゲーム設定が保持されている

### 境界ケーステスト

#### 翻訳未適用

- [ ] 翻訳がない場合、英語のフォールバック文字列が表示される
- [ ] `.mo` ファイルがない場合でもエラーにならない

#### データ移行

- [ ] 旧バージョンのゲームデータで新バージョンが動作する
- [ ] メタキーの変更がない（投稿タイプ・カスタムフィールド）

#### アップグレード

- [ ] 既存バージョンから新バージョンへのアップグレードが成功する
- [ ] アップグレード後、ゲームデータが保持されている

### セキュリティテスト

- [ ] 権限チェックが正しく機能する（一般ユーザーで確認）
- [ ] nonce 検証が正しく機能する
- [ ] SQL インジェクション対策が有効である（`$wpdb->prepare()` 使用）
- [ ] XSS 対策が有効である（`esc_html()` / `esc_attr()` 使用）

### パフォーマンステスト

- [ ] 管理画面の読み込みが遅延しない
- [ ] フロントエンドの表示が遅延しない
- [ ] データベースクエリが最適化されている

---

## トラブルシューティング

### 翻訳ファイル関連

#### 問題: .pot 再生成で既存の参照行が消える

**原因**: 該当の `__()` / `_e()` 呼び出しが削除された、またはコメント扱いになっている。

**解決策**:

```bash
# 該当ファイルで翻訳関数が使われているか確認
grep -n "__(" admin/target-file.php
```

#### 問題: 複数ファイルの参照が統合される

**原因**: 同じ msgid が複数ファイルで使用されている（これは正常）。

**解決策**: そのままで問題ありません。gettext の仕様です。

```po
#: admin/file1.php:10 admin/file2.php:20
msgid "Common message"
msgstr ""
```

#### 問題: .mo ファイル生成でエラーが出る

**原因**: .po ファイルに構文エラーがある。

**解決策**:

```bash
# 構文チェック
msgfmt -c -v -o /dev/null languages/novel-game-plugin-ja.po
```

### バージョン管理関連

#### 問題: バージョン番号が一致しない

**確認箇所**:

1. `novel-game-plugin.php` ヘッダー（6行目付近）
2. `NOVEL_GAME_PLUGIN_VERSION` 定数（26行目付近）
3. `languages/novel-game-plugin.pot` の Project-Id-Version

**解決策**: すべての箇所を同じバージョン番号に統一してください。

### Git 操作関連

#### 問題: fast-forward マージができない

**エラー例**:

```
fatal: Not possible to fast-forward, aborting.
```

**原因**: dev と master の履歴が分岐している。

**解決策**:

1. master と dev の履歴を確認：

```bash
git log --oneline --graph --all --decorate -10
```

2. 分岐の原因を特定：
   - master に直接コミットされていないか？
   - dev のコミットが巻き戻されていないか？

3. **絶対に rebase / force-push しないこと**

4. ユーザーに状況を報告し、手動での対応を依頼してください。

#### 問題: タグを誤って付けた

**解決策** （タグをまだ push していない場合）:

```bash
# ローカルのタグを削除
git tag -d vX.Y.Z

# 正しいコミットにタグを付け直す
git tag -a vX.Y.Z <correct-commit-hash> -m "Release version X.Y.Z"
```

**解決策** （タグを既に push した場合）:

```bash
# リモートのタグを削除（慎重に！）
git push --delete origin vX.Y.Z

# ローカルのタグを削除
git tag -d vX.Y.Z

# 正しいコミットにタグを付け直す
git tag -a vX.Y.Z <correct-commit-hash> -m "Release version X.Y.Z"

# タグを再度 push
git push origin vX.Y.Z
```

**注意**: タグの削除・再作成は、Release を作成する前に行ってください。Release 作成後の場合は、Release も削除してから対応してください。

### ZIP 作成関連

#### 問題: ZIP に不要なファイルが含まれる

**確認方法**:

```bash
unzip -l novel-game-plugin-X.Y.Z.zip | grep -E '\.git|tests|scripts|\.DS_Store'
```

**解決策**: ZIP を削除して、除外パターンを追加して再作成：

```bash
zip -r novel-game-plugin-X.Y.Z.zip novel-game-plugin/ \
  -x "*.git*" "*/.DS_Store" "*/tests/*" "*/scripts/*" "*/docs/*.md" "*~" "*.bak"
```

#### 問題: ZIP のサイズが異常に大きい

**確認方法**:

```bash
ls -lh novel-game-plugin-X.Y.Z.zip
# 目安: 500KB〜2MB程度
```

**原因**: 大きなファイル（画像・動画・ログ等）が含まれている。

**解決策**:

```bash
# ZIP の内容を詳細表示
unzip -l novel-game-plugin-X.Y.Z.zip | sort -k4 -n -r | head -20
```

大きなファイルを特定し、除外してください。

---

## 自動化提案

現在は手動でのリリースプロセスですが、将来的には以下の自動化が可能です。

### GitHub Actions によるリリース自動化

`.github/workflows/release.yml` の作成例：

```yaml
name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install dependencies
        run: sudo apt-get install -y gettext zip
      
      - name: Generate .mo files
        run: |
          msgfmt -o languages/novel-game-plugin-ja.mo languages/novel-game-plugin-ja.po
      
      - name: Create distribution ZIP
        run: |
          mkdir -p dist
          rsync -av --exclude='.git' --exclude='tests' --exclude='scripts' \
            --exclude='.github' . dist/novel-game-plugin/
          cd dist
          zip -r novel-game-plugin-${{ github.ref_name }}.zip novel-game-plugin/
          sha256sum novel-game-plugin-${{ github.ref_name }}.zip > \
            novel-game-plugin-${{ github.ref_name }}.zip.sha256
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/novel-game-plugin-${{ github.ref_name }}.zip
            dist/novel-game-plugin-${{ github.ref_name }}.zip.sha256
          body_path: RELEASE_NOTES.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

### 翻訳ファイル自動生成

WP-CLI を使った自動化例：

```yaml
- name: Generate POT file
  run: |
    docker run --rm -v $(pwd):/work \
      wordpress:cli i18n make-pot /work /work/languages/novel-game-plugin.pot \
      --domain=novel-game-plugin
```

### 自動テスト

リリース前の自動テスト例：

```yaml
- name: PHP Syntax Check
  run: find . -name "*.php" -exec php -l {} \;

- name: WordPress Coding Standards
  run: |
    composer require --dev wp-coding-standards/wpcs
    ./vendor/bin/phpcs --standard=WordPress .
```

---

## 参考リンク

### プロジェクト内ドキュメント

- [ブランチ運用ポリシー](.github/copilot-instructions.md#ブランチ運用ポリシーgit-workflow)
- [Issue 作成SOP](.github/ISSUE_CREATION_SOP.md)
- [開発者ガイド](DEVELOPER_GUIDE.md)
- [コードレビューチェックリスト](CODE_REVIEW_CHECKLIST.md)

### WordPress 関連

- [WordPress Plugin Handbook](https://developer.wordpress.org/plugins/)
- [WordPress Coding Standards](https://developer.wordpress.org/coding-standards/wordpress-coding-standards/)
- [WP-CLI i18n Commands](https://developer.wordpress.org/cli/commands/i18n/)

### Git / GitHub

- [Semantic Versioning 2.0.0](https://semver.org/)
- [Git Tagging](https://git-scm.com/book/en/v2/Git-Basics-Tagging)
- [GitHub Releases Documentation](https://docs.github.com/en/repositories/releasing-projects-on-github)

### 翻訳ツール

- [GNU gettext Manual](https://www.gnu.org/software/gettext/manual/gettext.html)
- [WordPress I18n for WordPress Developers](https://developer.wordpress.org/plugins/internationalization/)

---

## ガードレール（重要な禁止事項）

リリースプロセスにおいて、以下の操作は **絶対に行わないでください**：

### Git 操作の禁止事項

- ❌ `git push --force` / `git push --force-with-lease`
- ❌ `git rebase` による履歴の書き換え
- ❌ master / dev への直接コミット（PR を経由しない）
- ❌ タグの強制上書き

### 自動化の禁止事項

- ❌ GitHub Copilot Agent による自動 PR 作成（リリース関連）
- ❌ テンプレート外の Issue / PR 投稿
- ❌ 確認なしの自動マージ

### データ操作の禁止事項

- ❌ リリース済みタグの削除（Release 作成後）
- ❌ master ブランチの履歴改変
- ❌ 公開済み Release の削除

---

**最終更新**: 2025-12-18  
**バージョン**: 1.0.0
